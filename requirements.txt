# SimSID -- minimal modern stack, derived from the repository's actual imports.
# environment.yml pins the original Python 3.6 / CUDA 10.0 environment for the record.
torch>=2.0
torchvision>=0.15
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.20
opencv-python>=4.8
Pillow>=10.0
matplotlib>=3.7
tqdm>=4.65
tensorboard>=2.13
